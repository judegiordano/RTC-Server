/* eslint-disable no-unused-vars */

export const enum Endpoints {
	devUrl = "https://jsonplaceholder.typicode.com/todos",
	prodUrl = "https://api.mocki.io/v1/b043df5a"
}

export const enum Env {
	dev = "development",
	prod = "production"
}

export const enum Domain {
	lcoal = "127.0.0.1"
}